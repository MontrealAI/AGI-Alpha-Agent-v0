# α‑AGI Marketplace Demo configuration
# Copy to `config.env` and edit as needed.

# Optional — leave blank to run fully offline via Mixtral
OPENAI_API_KEY=

# Comma‑separated list of agents to launch
ALPHA_ENABLED_AGENTS=finance,energy

# Exposed REST port
PORT=8000
