# Root dependencies (minimal install)
-r alpha_factory_v1/requirements-core.txt

# Windows-only dependency of the MCP transport, pinned across supported Python versions.
pywin32==311 ; platform_system == "Windows"
