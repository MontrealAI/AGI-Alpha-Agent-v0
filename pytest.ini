[pytest]
markers =
    e2e: end-to-end integration tests
    smoke: quick core dependency health checks
    asyncio: mark a test that uses asyncio
    ablation: evaluate agent ablation results
testpaths = tests
