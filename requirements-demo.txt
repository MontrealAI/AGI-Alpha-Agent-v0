# Extra packages required for demo-related tests
# Keep versions loosely pinned so heavy wheels resolve quickly
torch>=2.2
gymnasium[classic-control]>=0.29
gradio>=5.38.0
filelock>=3.13
openai-agents>=0.0.17,<0.2
tqdm~=4.67

