# Build and publish the Insight docs bundle whenever related sources change.
# Regenerates docs/alpha_agi_insight_v1/insight.bundle.js and commits the result.
name: build-insight-bundle

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'docs/alpha_agi_insight_v1/**'
      - 'alpha_factory_v1/demos/alpha_agi_insight_v1/**'
      - 'package.json'
      - 'package-lock.json'
      - 'vite.config.*'
      - 'rollup.config.*'
      - 'webpack.config.*'

jobs:
  build-insight-bundle:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: alpha_factory_v1/demos/alpha_agi_insight_v1/insight_browser_v1/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: alpha_factory_v1/demos/alpha_agi_insight_v1/insight_browser_v1

      - name: Detect build script
        id: build-script
        run: |
          scripts=("build:docs-insight" "build:insight" "build")
          for script in "${scripts[@]}"; do
            value=$(npm pkg get "scripts.${script}" | tr -d '"')
            if [ "${value}" != "null" ]; then
              echo "script=${script}" >> "$GITHUB_OUTPUT"
              exit 0
            fi
          done
          echo "No build script found." >&2
          exit 1
        working-directory: alpha_factory_v1/demos/alpha_agi_insight_v1/insight_browser_v1

      - name: Build Insight bundle
        run: npm run ${{ steps.build-script.outputs.script }}
        working-directory: alpha_factory_v1/demos/alpha_agi_insight_v1/insight_browser_v1

      - name: Sync built assets into docs
        run: |
          rsync -av --delete dist/ ../../../../docs/alpha_agi_insight_v1/
        working-directory: alpha_factory_v1/demos/alpha_agi_insight_v1/insight_browser_v1

      - name: Commit regenerated bundle
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: auto-regenerate docs Insight bundle"
          file_pattern: docs/alpha_agi_insight_v1/**
