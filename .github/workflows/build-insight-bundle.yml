# Regenerates docs/alpha_agi_insight_v1/insight.bundle.js when relevant sources change
# and auto-commits updated artifacts back to the repository.
name: "📦 Build Insight Bundle"

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "docs/alpha_agi_insight_v1/**"
      - "alpha_factory_v1/demos/alpha_agi_insight_v1/**"
      - "package.json"
      - "package-lock.json"
      - "vite.config.*"
      - "rollup.config.*"
      - "webpack.config.*"

permissions:
  contents: write

jobs:
  build-insight-bundle:
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash
        working-directory: alpha_factory_v1/demos/alpha_agi_insight_v1/insight_browser_v1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Detect build script
        id: select-script
        run: |
          set -euo pipefail
          BUILD_SCRIPT=$(node --input-type=module <<'NODE'
          import { readFileSync } from 'fs';

          const pkg = JSON.parse(readFileSync('package.json', 'utf8'));
          const candidates = ['build:docs-insight', 'build:insight', 'build'];
          const chosen = candidates.find((name) => pkg.scripts?.[name]);

          if (!chosen) {
            console.error('No suitable build script found in package.json');
            process.exit(1);
          }

          console.log(chosen);
          NODE
          )
          echo "script=$BUILD_SCRIPT" >> "$GITHUB_OUTPUT"

      - name: Build Insight docs bundle
        env:
          BUILD_SCRIPT: ${{ steps.select-script.outputs.script }}
        run: |
          set -euo pipefail
          echo "Using npm script: $BUILD_SCRIPT"
          npm run "$BUILD_SCRIPT"

      - name: Commit regenerated bundle
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: auto-regenerate docs Insight bundle"
          file_pattern: docs/alpha_agi_insight_v1/**
