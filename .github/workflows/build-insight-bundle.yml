# Regenerates the α‑AGI Insight docs bundle when relevant files change.
# The workflow rebuilds the Insight browser assets and commits the refreshed
# docs/alpha_agi_insight_v1/insight.bundle.js back to the repository.
name: build-insight-bundle

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'docs/alpha_agi_insight_v1/**'
      - 'alpha_factory_v1/demos/alpha_agi_insight_v1/**'
      - 'package.json'
      - 'package-lock.json'
      - '**/vite.config.*'
      - '**/rollup.config.*'
      - '**/webpack.config.*'

permissions:
  contents: write

jobs:
  build-insight-bundle:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: alpha_factory_v1/demos/alpha_agi_insight_v1/insight_browser_v1

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Determine docs Insight build script
        id: build-script
        run: |
          SCRIPT=$(node <<'NODE'
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            const order = ['build:docs-insight', 'build:insight', 'build'];
            const found = order.find((name) => pkg?.scripts?.[name]);
            if (!found) {
              console.error('No suitable build script found in package.json');
              process.exit(1);
            }
            console.log(found);
          NODE
          )
          echo "script=${SCRIPT}" >> "$GITHUB_OUTPUT"

      - name: Install dependencies
        run: npm ci

      - name: Build Insight docs bundle
        env:
          BUILD_SCRIPT: ${{ steps.build-script.outputs.script }}
          DOCS_DIR: ${{ github.workspace }}/docs/alpha_agi_insight_v1
        run: |
          npm run "$BUILD_SCRIPT"
          OUT_DIR="$PWD/dist"
          mkdir -p "$DOCS_DIR"
          rsync -a --delete "$OUT_DIR/" "$DOCS_DIR/"

      - name: Commit updated Insight bundle
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: auto-regenerate docs Insight bundle"
          file_pattern: docs/alpha_agi_insight_v1/**
